# package name space mapping mechanism using an `@maintainer` scope syntax
# uses underlying npm package download schemes and semver to allow:
#  - use of non registry published packages on a git or https endpoint.
#  - use of private registry.
---
packages:
  'safetag-toolkit': safetag-toolkit@^0.4.0
  # '@safetag/toolkit': contentascode/safetag-toolkit#semver:^0.1.0
  # '@safetag/guide': safetag-guide@1.0.0
  # '@safetag/curricula': safetag-curricula@1.0.0
